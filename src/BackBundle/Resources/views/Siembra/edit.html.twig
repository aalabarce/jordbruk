{% extends '::base.html.twig' %}

{% block body %}
    <section class="container">
        <h4 class="header up">Editar siembra</h4>
        <hr>
        <div class="adresses col-sm-6"> 
            <form id="siembra_form" action="{{url('siembra_update', { 'id': siembra.id })}}" method="post">
                {% include 'BackBundle:Siembra:form.html.twig' with {'form':form } %}
            </form>
        </div>
    </section>
{% endblock %}