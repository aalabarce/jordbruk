{{ form_start(form) }}
    {{ form_row(form.nombre) }}
    {{ form_row(form.superficie) }}
    {{ form_row(form.suelo) }}
    {{ form_row(form.descripcion) }}

    <button type="submit">Guardar</button>
    <a class="btn btn-default" href="{{url('lote')}}">Cancelar</a>                    
{{ form_end(form) }}